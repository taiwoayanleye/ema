<div class="contain-to-grid">
  <nav class="top-bar" data-topbar role="navigation">
      <!-- Left Nav Section and Toggle Menu -->
    <ul class="title-area">
      <div class="name" >
        <ul class="left">
          <div class ="logo">
            <!-- <a href='/'><%# image_tag 'stutern_logo.png' %></a> -->
            <% if current_user.present? %>
              <% if current_user.user_type == "company" %>
                <% if current_user.profile.verified == true %>
                  <h1><%= link_to image_tag('https://s3-us-west-2.amazonaws.com/stuternbeta/stutern_logo.png'), '/student_profiles' %></h1>
                <% else %>
                  <h1><%= link_to image_tag('https://s3-us-west-2.amazonaws.com/stuternbeta/stutern_logo.png'), '/' %></h1>
                <% end %>
              <% elsif current_user.user_type == "student" %>
                <h1><%= link_to image_tag('https://s3-us-west-2.amazonaws.com/stuternbeta/stutern_logo.png'), '/job_postings' %></h1>
              <% end %>
            <% else %>
              <h1><%= link_to image_tag('https://s3-us-west-2.amazonaws.com/stuternbeta/stutern_logo.png'), '/' %></h1>
            <% end %>
          </div>
        </ul>
        <!-- Remove the class "menu-icon" to get rid of menu icon. Take out "Menu" to just have icon alone -->
        <li class="toggle-topbar menu-icon"><a href="#"><span></span></a></li>
      </div>
    </ul>
    <!-- Right bar -->
    <section class="top-bar-section">
      <!-- Righthandside navbar -->
      <ul class="right">
        <%= render 'devise/menu/login_items' %>
        <%= render 'devise/menu/registration_items' %>
        <%= render 'devise/menu/common_user_items' %>
      </ul>
    </section>
  </nav>
</div>
