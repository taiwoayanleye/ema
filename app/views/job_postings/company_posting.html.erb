<% if (user_signed_in? && current_user.user_type == "company") %>
<%= link_to 'New Job Posting', new_job_posting_path, class: "btn btn-link" %>
<% end %>
<% @job_postings.each do |job_posting| %>
<div class="large-12 medium-12 small-12 columns">
  <div id='manage_postings'>

    <div id="int_position_container">
      <dl>
        <dd> Position </dd>
        <dt id="intern_position_content"><%= job_posting.position %></dt>
      </dl>
    </div>

    <div id="int_duration_container">
      <dl>
        <dd> Duration </dd>
        <dt id="intern_position_content"><i class="fa fa-calendar"></i><%= "#{job_posting.company_profile.number_of_employees}" %></dt>
      </dl>
    </div>

  </div>

    <!-- Single button
    <div class="btn-group">
      <button type="button" class="btn btn-primary dropdown-toggle" data-toggle="dropdown">Edit<span class="caret"></span>
      </button>
      <ul class="dropdown-menu" role="menu">
        <li><%= link_to "View", job_posting, class: "btn btn-link" %></li>
        <li><%= link_to "Edit", edit_job_posting_path(job_posting), class: "btn btn-link" %></li>
        <li><%= link_to "Destroy", job_posting, method: :delete, data: { confirm: "Are you sure?" }, class: "btn btn-link" %></li>
      </ul>
    </div>
    -->
</div> 
<% end %>


