<section class="top-bar-section">
	<ul class="right">
		<% if user_signed_in? %>
		  <li>
		  	<%= link_to 'Edit registration', edit_user_registration_path, :class => 'navbar-link' %>
		  </li>
		 <% else %>
		   <!-- <li class="has-dropdown"> -->
	        <!-- <a href="#">Register</a> -->
	        <!-- <ul class="dropdown"> -->
	          <li>
	          	<%= link_to 'Candidate', new_user_registration_path(user_type: "student"), :class => 'navbar-link' %>
	          </li>
	          <li>
	          	<%= link_to 'Employer', new_user_registration_path(user_type: "company"), :class => 'navbar-link' %>
	          </li>
	        <!-- </ul> -->
	      <!-- </li> -->
		<% end %>
	</ul>
</section>