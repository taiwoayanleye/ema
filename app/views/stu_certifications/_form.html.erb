<%= form_for(@stu_certification) do |f| %>
  <% if @stu_certification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stu_certification.errors.count, "error") %> prohibited this stu_certification from being saved:</h2>

      <ul>
      <% @stu_certification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
